# license-management

Engineering workspace initialized with Python 3.12 and uv dependency management.

## Launch

- GUI mode (default entry): `uv run python -m license_management`
- Version only: `uv run python -m license_management --version`
- Install PyQt5 before GUI launch.
